<!-- Checkbox (Table Header) -->
<div class="form-check">
  <input type="checkbox"
         class="<%= input_class %>"
         name="<%= input_name %>"
         id="<%= input_id %>" />
  <label class="form-check-label">
    &nbsp;
  </label>
</div>
